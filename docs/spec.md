# terminal-app 要件定義書

| 項目 | 内容 |
|------|------|
| 版 | 0.1（初版） |
| 作成日 | 2026-07-11 |
| 正とする入力 | 壁打ちノート `C:/Users/hppym/dev/obsidian-vault/40-dev-notes/terminal-app/terminal-app初期構想.md` / UI モック観察記録 `.loop/current/context-design-mock.md` |
| 関連文書 | 詳細設計書 `design.md` / 検証方法 `verification.md` |

本書の要件には ID（REQ-xx: 機能要件、NFR-xx: 非機能要件、AC-xx: 受入基準、OPEN-xx: 未確定事項）を振る。`design.md` の各章は対応する REQ/NFR を明記し、`verification.md` は AC ごとの検証項目（V-xx）を対応表で管理する。

## 1. 背景・目的・成功基準

### 1.1 背景

Claude Code CLI を約 6 プロジェクト並行で稼働させており、完了通知がどのプロジェクトのものか識別しづらい。完了・許可待ちに気づくのが遅れると、その分だけ並行作業全体が停滞する。

### 1.2 目的

**どのセッションが止まったかをひと目で識別し、1 クリックで対象ウィンドウに切り替える**ための常駐タイルダッシュボードアプリ（Windows 11）を作る。

### 1.3 成功基準

| ID | 成功基準 | 確認方法 |
|----|----------|----------|
| SC-1 | 並行セッションの状態変化（完了・確認待ち・エラー）に、視界の隅でも気づける（発光表現） | AC-04〜AC-07 |
| SC-2 | タイル 1 クリックで対象ウィンドウ（Cursor またはターミナル）が前面化する | AC-09 |
| SC-3 | プロジェクト登録が D&D だけで完了し、hooks 設定を手作業で編集しない | AC-01, AC-02 |

## 2. 利用者・利用シーン

- **利用者**: 本人のみ（開発者 1 名）。多人数利用・配布は想定しない。
- **環境**: Windows 11 ローカル。ネットワーク公開なし。

| シーン | 期待する挙動 |
|--------|--------------|
| 複数プロジェクトで Claude Code を並行起動し、別作業をしている | タイルは静かに「実行中」表示（スピナー＋経過時間）で邪魔をしない |
| どれかのセッションが応答を終えた | 該当タイルが「完了」（緑）で呼吸発光し、視界の隅で気づける |
| ツール実行の許可や質問で停止した | 該当タイルが「確認待ち」（アンバー）で呼吸発光する（放置防止） |
| セッションが異常終了した | 該当タイルが「エラー」（静的な赤）になる |
| 気づいたタイルをクリックする | 設定済みの対象（Cursor／ターミナル）のウィンドウが前面化する |
| 新しいプロジェクトを監視したい | フォルダをウィンドウへ D&D するだけで登録が完了する |

## 3. スコープ

### 3.1 やること（MVP = コアのみ）

**D&D 登録 → 状態検知 → タイル点灯 → クリックで前面化**（壁打ちノートの MVP 線引きどおり）。

### 3.2 やらないこと（MVP 外）

| 項目 | 位置づけ |
|------|----------|
| OS トースト通知・サウンド | **次期（should）**。希望あり・設定で ON/OFF できる前提。モック 1d に通知音トグルがあるが、MVP では無効化して提供する（REQ-12、design.md 6.4 参照） |
| 完了履歴・タイムライン表示 | 次期候補（could） |
| トレイ常駐時のコンパクト 1 行バー表示 | モック側「次の一手」メモの調整候補。未確定（OPEN-05） |
| macOS / Linux 対応 | 対象外（Windows 11 のみ） |
| ネットワーク公開・複数人利用 | 対象外（本人のみ・ローカル） |
| Claude Code 以外のツールの監視 | 対象外 |

## 4. 機能要件

優先度は must（MVP）/ should（次期）/ could（次期候補）。

| ID | 優先度 | 要件 | 受入基準 |
|----|--------|------|----------|
| REQ-01 | must | フォルダをメインウィンドウへドラッグ&ドロップするとプロジェクトが登録され、タイルが追加される | AC-01 |
| REQ-02 | must | 登録時に対象プロジェクトの `.claude/settings.json` へ hooks を自動追記する。既存設定は壊さずマージし、登録解除時は自アプリ分のみ除去する | AC-02, AC-03 |
| REQ-03 | must | Claude Code の hooks（Stop / Notification イベント）でセッション状態を検知する | AC-04, AC-05, AC-16 |
| REQ-04 | must | タイルは 4 状態（実行中・完了・確認待ち・エラー）を色＋アイコンで区別し、完了・確認待ちは呼吸発光、エラーは静的表示とする | AC-04〜AC-07, AC-16 |
| REQ-05 | must | タイルクリックで該当プロジェクトのウィンドウを前面化する | AC-09 |
| REQ-06 | must | プロジェクトごとに前面化対象（Cursor エディタ／ターミナル）を設定画面で選択できる | AC-10 |
| REQ-07 | must | 常駐デスクトップアプリ（小さめのタイルウィンドウ）。「常に手前」を ON/OFF できる | AC-11 |
| REQ-08 | must | タイルにプロジェクト名／状態アイコン／状態ラベル＋相対時刻（または実行中の経過時間）を表示する | AC-08 |
| REQ-09 | must | プロジェクト 0 件時は空状態（D&D 誘導）を表示する | AC-12 |
| REQ-10 | must | ステータスバーに状態別件数と総セッション数を表示する（例「8実行中 2完了 1確認待ち 1エラー / 12セッション」） | AC-13 |
| REQ-11 | must | プロジェクトの登録解除ができる（hooks 除去を含む） | AC-03 |
| REQ-12 | should（次期） | OS トースト通知・サウンド（完了・確認待ちで音を鳴らす。設定で ON/OFF）。MVP では設定 UI を無効状態で表示し、音は鳴らさない | AC-18 |
| REQ-13 | should（次期） | テーマ切替（ライト／ダーク／自動）。UI モックはライト・ダーク両対応で作成済み。MVP はダーク基調を既定とし、切替実装は次期でも可 | AC-17 |
| REQ-14 | could（次期候補） | 完了履歴・タイムライン表示 | —（次期候補のため AC なし） |
| REQ-15 | must | タイルをドラッグ&ドロップで並べ替えられ、並び順はアプリ再起動後も保持される（260906_1 #2） | AC-19 |
| REQ-16 | must | タイトルバーの「自動整列」で、接続中（対象アプリのウィンドウがある、または実行中・確認待ち）のタイルを先頭（左上）へ、未接続を後ろへ寄せる。各グループ内の相対順は維持する（260906_1 #1） | AC-20 |
| REQ-17 | must | Claude がまだ作業中（Stop hook が block されて続行した／同期 fork・codex 待ちで本体 transcript が止まっている）なら、タイルを「完了」「切断」にせず「実行中」に保つ（戻す）。根拠は Claude Code の登録簿 status=busy、transcript の block 痕跡、subagent 記録の更新（260907_1） | AC-21 |
| REQ-18 | must | タイルに品質ループ（eval-loop）の進捗バッジを出す（例「ループ 2/4・codex 実装中 1分・最高 78点」。終了後 30 分は「ループ終了・合格 92点」）。情報源は eval-loop の registry と state.json、codex ジョブの heartbeat（260907_2） | AC-22 |
| REQ-19 | must | 確認待ちのタイルは、並び順に関係なくグリッドの先頭（左上）に表示する。表示上の並びだけで projects.json の並び順は変えず、確認待ちが解ければ元の位置へ戻る（260922_1） | AC-23 |
| REQ-20 | should | Jev（TypeSafe AI の判断専用モデル）を制御層の門番として使い、(1) Stop 後に最後の返答が質問・判断依頼で終わっていれば「完了」でなく「返答待ち」（確認待ちと同じ表示）にする (2) 権限確認のツール呼び出しが取り消せない・外部へ送る・広範囲なら赤い印を出す (3) 短いプロンプト（「はい」「A」等）で作業テキストを上書きしない (4) 実行中の直近手順が同じ失敗の繰り返し・進展なしなら停滞の印を出す。キー無し・env TERMINAL_APP_JEV=off・API 失敗時は従来どおり動く（260922_2） | AC-24 |
| REQ-21 | must | 起動時に登録済み全プロジェクトの transcript を走査し、登録簿で生きているセッションを「完了」「実行中」で復元する。完了で復元したセッションは Jev で返答待ちかを一括判定する。手動の「再接続」も同じ判定を行う（260922_3） | AC-25 |
| REQ-22 | must | 「返答待ち」は実質「完了」なので、完了・切断と同じ根拠（登録簿 status=busy／Stop hook の block 痕跡／transcript 終端が open＝ターン再開）で「実行中」へ戻す。返答待ちにする直前にも同じ根拠で見送る（260922_4） | AC-26 |
| REQ-23 | should | Jev でタイルの表示名と作業内容の整合を判定し、見直した方がよいタイルに印を出す。右クリックから Claude Code CLI（sonnet）に新しい表示名を作らせ、確認のうえ改名できる（260922_6） | AC-27 |

## 5. データ・業務ルール

### 5.1 タイルの 4 状態の定義

| 状態 | 意味 | 主な検知イベント | 表示（モック準拠） |
|------|------|------------------|--------------------|
| 実行中 | Claude Code が作業中 | hooks: UserPromptSubmit（プロンプト送信。OPEN-04 解消 — design.md 4.5、2026-07-11 案 A 採用） | ニュートラル＋スピナー＋経過時間（例 1:24:01） |
| 完了 | 応答が終わり入力待ちに戻った（**最も目立たせる**） | hooks: Stop | 緑 ✓＋呼吸発光＋相対時刻（「完了・2分前」） |
| 確認待ち | ツール実行の許可や質問で停止中（放置すると進まないため通知価値が高い） | hooks: Notification | アンバー ?＋呼吸発光＋相対時刻 |
| エラー | 異常終了 | 検知の網羅範囲は OPEN-03（design.md 4.4） | 静的な赤 ⚠（発光アニメーションなし）＋相対時刻 |

補助表示: 登録直後でイベント未受信のタイルは、4 状態のいずれでもない「待機（イベント待ち）」としてニュートラル無発光で表示する（4 状態の定義は変えない。design.md 5.1 参照）。

### 5.2 状態遷移（概要）

登録 →（待機）→ 実行中 ⇄ 確認待ち → 完了 → 実行中 …、任意の状態からエラーへ遷移しうる。遷移表の正本は design.md 5.1。

テキスト遷移図（2026-07-11 追記。`[ ]` = 状態、`--x-->` = 入力 x による遷移）:

```
登録 --> [待機]
[任意の状態] --Stop--> [完了]                     （エラーからの復帰を含む）
[任意の状態] --Notification--> [確認待ち]         （確認待ち中は時刻のみ更新）
[任意の状態] --実行開始(OPEN-04)--> [実行中]      （実行中の継続は起点を維持）
[任意の状態] --エラー検知(OPEN-03)--> [エラー]
アプリ再起動 --> 全タイル [待機]（セッション状態は揮発）
```

### 5.3 hooks イベントとの対応

| hooks イベント | 遷移先状態 |
|----------------|-----------|
| Stop | 完了 |
| Notification（許可要求・入力待ち通知） | 確認待ち |
| UserPromptSubmit（プロンプト送信。2026-07-11 OPEN-04 案 A 採用で追加） | 実行中 |

マッピング詳細（Notification の種別判定、エラー・実行中の扱い）は design.md 4.3〜4.5 を正本とする。

### 5.4 複数セッションの扱い

同一プロジェクトで複数セッションが並行する場合、**直近イベントのセッションを優先表示**する（壁打ちノートの前提。詳細規則は design.md 5.2）。

## 6. 非機能要件

| ID | 分類 | 要件 | 受入基準 |
|----|------|------|----------|
| NFR-01 | 性能 | イベント発火からタイル反映まで 1 秒以内（ローカル通信） | AC-04 |
| NFR-02 | 無害性 | 本アプリが未起動・応答不能でも Claude Code セッションを阻害しない（hook コマンドは短いタイムアウトで失敗しても本体を止めない） | AC-14 |
| NFR-03 | 安全性 | `.claude/settings.json` を破壊しない（追記前バックアップ・アトミック書き込み・既存 hooks 保全。パース不能時は書き込まない） | AC-02, AC-03 |
| NFR-04 | セキュリティ | イベント受信は 127.0.0.1 バインドのみ。外部公開しない | V-19（verification.md） |
| NFR-05 | レイアウト | タイル最大 12〜16 でレイアウトが破綻しない | AC-15 |
| NFR-06 | 視認性 | 発光は点滅ではなく約 2.4 秒周期の「呼吸」。エラーは動かない赤（= 止まっている記号）。**例外（260904_1 #2）**: 確認待ちだけはユーザー指示により青（アクセント色）の 1 秒周期の点滅とし、見落としを防ぐ | AC-06 |
| NFR-07 | 常駐負荷 | アイドル時に CPU をほぼ消費しない（目安: アイドル時 CPU 1% 未満） | V-20（verification.md） |

## 7. 制約・前提

- Windows 11 / 利用者は本人のみ / ローカル動作・ネットワーク公開なし。
- タイル数は最大 12〜16 を想定してレイアウト設計（現状 6 個＋余裕）。
- 検知は Claude Code の hooks 仕様（`.claude/settings.json`、Stop / Notification イベント）に依存する。
- UI はモック（面 1a〜1f、`context-design-mock.md`）を正とする。モック側「次の一手」メモは未反映の調整候補として扱う（OPEN-05）。
- **検討して却下した案**（再検討しない前提）: ローカル Web アプリ形態（他アプリの前面化に制約があるため常駐デスクトップを採用）／hooks の手動設定（「簡単な UI/UX」に反するため自動追記を採用）。

## 8. 受入基準

各 AC は `verification.md` の検証項目（V-xx）と 1 対 1 以上で対応づける。

| ID | 優先度 | 受入基準（検証可能な形） | 対応要件 |
|----|--------|--------------------------|----------|
| AC-01 | must | フォルダをメインウィンドウへ D&D すると、そのプロジェクトのタイルが追加され、アプリ再起動後も登録が保持されている | REQ-01 |
| AC-02 | must | D&D 登録後、対象プロジェクトの `.claude/settings.json` に Stop / Notification の hooks エントリが追記されており、登録前から存在した設定・hooks が変化していない | REQ-02, NFR-03 |
| AC-03 | must | 登録解除すると、本アプリが追記した hooks エントリのみが除去され、それ以外の内容が登録前と一致する | REQ-02, REQ-11, NFR-03 |
| AC-04 | must | 登録プロジェクトで Claude Code の応答が完了（Stop 発火）すると、1 秒以内に該当タイルが「完了」（緑 ✓）になる | REQ-03, REQ-04, NFR-01 |
| AC-05 | must | 許可要求などで Notification が発火すると、該当タイルが「確認待ち」（青 ? 点滅。260904_1 #2 でアンバーの呼吸から変更）になる。許可後に作業が再開された（transcript 更新／Claude Code 登録簿 status=busy）ことを 15 秒周期の掃引で検知し「実行中」へ自動で戻る | REQ-03, REQ-04 |
| AC-06 | must | 完了のタイルは約 2.4 秒周期の呼吸発光、確認待ちのタイルは青の 1 秒周期の点滅（260904_1 #2）をし、エラーのタイルは発光アニメーションのない静的な赤で表示される | REQ-04, NFR-06 |
| AC-07 | must | 実行中のタイルはニュートラル色＋スピナーで表示され、呼吸発光しない | REQ-04 |
| AC-08 | must | タイルにプロジェクト名／状態アイコン（✓ ? ⚠ スピナー）／状態ラベル＋相対時刻（実行中は経過時間）が表示される | REQ-08 |
| AC-09 | must | タイルクリックで、そのプロジェクトに設定された対象（Cursor またはターミナル）のウィンドウが前面化する。最小化されていた場合は復元して前面化する | REQ-05, REQ-06 |
| AC-10 | must | 設定画面でプロジェクトごとに Cursor／ターミナルを切り替えられ、次回クリックから反映される | REQ-06 |
| AC-11 | must | 「常に手前」を ON にするとウィンドウが最前面固定になり、OFF で解除される | REQ-07 |
| AC-12 | must | プロジェクト 0 件のとき「フォルダをここにドラッグ&ドロップ」の空状態が表示される | REQ-09 |
| AC-13 | must | ステータスバーに状態別件数と総セッション数が表示され、状態変化に追随して更新される | REQ-10 |
| AC-14 | must | 本アプリ未起動の状態で Claude Code セッションを実行しても、エラーなく完走する（hook の失敗がユーザー操作を要求しない） | NFR-02 |
| AC-15 | must | 16 プロジェクト登録時にタイルグリッドが破綻せず、全タイルが視認・クリックできる | NFR-05 |
| AC-16 | must | エラーを示すイベントを受信した場合、該当タイルが「エラー」（静的な赤 ⚠）になる（検知できる範囲は OPEN-03 に従う） | REQ-03, REQ-04 |
| AC-17 | should | テーマをライト／ダーク／自動で切替でき、モック 1e・1f 相当の配色になる | REQ-13 |
| AC-18 | should（MVP では逆向きに検証） | MVP ビルドでは通知音設定 UI が無効状態（操作不可＋次期対応の明示）であり、いかなるイベントでも音が鳴らない | REQ-12 |
| AC-19 | must | タイルを別のタイルの左半分／右半分へドロップすると、その手前／直後へ移動し、再起動後も同じ並びで表示される。分割タイル（①②）はプロジェクト単位で一緒に動き、フォルダのドロップ（登録）は従来どおり動く（260906_1 #2） | REQ-15 |
| AC-20 | must | 「自動整列」を押すと接続中のタイルが左上から順に並び、未接続タイルが末尾へ移る。すでに整列済みなら並びは変わらず、その旨がステータスバーに出る（260906_1 #1） | REQ-16 |
| AC-21 | must | (1) Stop 受信で「完了」になった後も登録簿 status が busy なら約 3.5 秒以内に「実行中」へ戻り、完了トーストは出ない。status が idle なら「完了」のまま。(2) 登録簿 status の無いセッションでも transcript に block 痕跡（preventedContinuation=true）が書かれれば「実行中」へ戻り、作業テキストが block 理由のラベルになる。(3) 本体 transcript が切断閾値を超えて無更新でも、登録簿 busy または subagent 記録の更新があれば「切断」にならない。切断後に subagent 記録が更新されれば「実行中」へ戻る（260907_1） | REQ-17 |
| AC-23 | must | 確認待ちになったタイルはグリッドの先頭（左上）へ移り、複数あれば元の相対順で先頭に並ぶ。分割タイル（①②）はプロジェクト単位で一緒に動く。許可して「実行中」に戻ると元の位置へ戻り、projects.json の並び順は変わらない（260922_1） | REQ-19 |
| AC-24 | should | (1) 日本語の質問で終わる返答の Stop は約 4 秒以内に「返答待ち・N分前」（青の点滅・左上）になり「返答が必要です」のトーストが出る。完了報告の Stop は「完了」のまま。(2) `rm -rf` / `git push --force` の権限確認には「取り消せない操作」等の赤い印が付き、Read / `npm test` には付かない。(3) 「はい」「A」「続けて」を送っても作業テキストは前の依頼文のまま。(4) 同じテストの失敗を 4 回以上繰り返す実行中タイルに「停滞の疑い」の赤い印が付き、進展すれば消える。(5) キー無し・TERMINAL_APP_JEV=off・API 失敗ではこれらが出ないだけで他の表示は変わらない。証跡: docs/evidence/20260922-jev/probe-result.json（実 API 14 件）（260922_2） | REQ-20 |
| AC-25 | must | アプリ再起動後、質問して止まっていた生存セッションのタイルが数秒以内に「返答待ち」（青の点滅・左上）で復元され、完了報告で止まっていたものは「完了」、作業中のものは「実行中」になる。ターミナルを閉じた古い transcript は復元されない。証跡: 2026-09-22 実測（22 プロジェクト → 復元 17・返答待ち 5）（260922_3） | REQ-21 |
| AC-26 | must | 返答待ちのタイルは、ユーザーが返答した／Claude が続行した（終端 open）／登録簿 busy／block 痕跡のいずれかで 15 秒以内に「実行中」へ戻る。Stop 直後の後片付けの書き込みだけでは戻らない。Jev の判定を待つ間に作業が再開していたら返答待ちにせず、その旨をログに残す。Stop hook が発火しない経路（割り込み・hook 未設定）で「完了」にしたセッションも返答待ちかを判定する（260922_4） | REQ-22 |
| AC-27 | should | 表示名が汎用的（実測: dev = name_generic 0.98）または作業と不一致のタイルに「✎ 名前が作業を表していません／名前と作業が一致しません」の印が出る。内部マーカー（task-notification 等）が作業テキストのときは出ない。右クリック →「表示名を AI に提案（Claude Sonnet）」で候補が出て、確認ダイアログで「この名前にする」を選ぶと改名され印が消える。CLI 不在・時間切れ・形式不正では表示名を変えない（260922_6） | REQ-23 |

## 9. 未確定事項（決め方付き）

| ID | 未確定事項 | 現時点の扱い | 決め方 |
|----|-----------|--------------|--------|
| OPEN-01 | 技術スタック（Electron / Tauri 等） | design.md 2 章で複数案併記＋推奨を提示 → **2026-07-11 ユーザー選択により Electron で確定（解消）** | 実装着手時に Claude Code が比較提案し、ユーザーが選択する（実施済み） |
| OPEN-02 | トースト・サウンドの詳細（音の種別、イベント別の出し分け） | 次期スコープ。MVP では UI 無効（REQ-12） | 次期スコープ着手時に決定 |
| OPEN-03 | エラー検知の網羅範囲（プロセスクラッシュは hooks だけでは拾えない可能性） | design.md 4.4 で候補案を併記。MVP は「検知できた範囲のみエラー表示」 | 実装時に技術検証 |
| OPEN-04 | 「実行中」への遷移の検知方式（UserPromptSubmit hook 追加の要否） | design.md 4.5 で案 A/B を併記＋推奨 → **2026-07-11 ユーザー実測フィードバック（返信＝Stop では状態が変わるが、プロンプト送信では何も変わらない）により案 A（UserPromptSubmit hook の追加追記）を採用（解消）**。既存登録プロジェクトへは起動時追補で再登録なしに適用（design.md 4.2） | 実装時に技術検証（実施済み: ユーザー実測＋擬似注入・起動時追補の実測 — `docs/verification-results.md`） |
| OPEN-05 | モック調整候補（発光の強さ/速さ、タイル 80px×16 面、トレイ常駐時のコンパクト 1 行バー） | 要件化しない（未反映の調整候補のまま） | モック確認の場でユーザーが判断 |

## 10. トレーサビリティの規約

- 要件は REQ-xx / NFR-xx、受入基準は AC-xx、未確定は OPEN-xx で採番する（本書が正本）。
- `design.md` は各章の冒頭に「対応要件」を明記する（REQ ⇔ 設計セクション対応表は design.md 11 章）。
- `verification.md` は AC ⇔ 検証項目（V-xx）の対応表を持つ（1 件以上の欠落も許さない）。
- 画面設計はモック面番号（1a〜1f）で参照する。
